version: 2
run:
  tests: true
linters:
  enable:
    - errcheck
    - unused
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
