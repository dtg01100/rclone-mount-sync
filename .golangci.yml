version: 2
run:
  tests: true
linters:
  enable:
    - errcheck
    - unused
    - govet
    - ineffassign
    - staticcheck
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
